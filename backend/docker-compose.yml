services:
  postgres:
    image: postgres:16
    container_name: spotify-game-db
    environment:
      POSTGRES_USER: spotify
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spotify_game
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: